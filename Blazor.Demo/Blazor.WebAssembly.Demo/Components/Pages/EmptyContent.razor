@page "/empty-content"

<h3>Empty Content Example</h3>

<Virtualize Items="@stringList">
    <ItemContent>
        <p>@context</p>
    </ItemContent>
    <EmptyContent>
        <p>There are no strings to display.</p>
    </EmptyContent>
</Virtualize>

@code {
    private List<string>? stringList = new();

    protected override void OnInitialized()
    {
        // for (int i = 0; i < 50; i++)
        // {
        //     stringList.Add($"item:{i}");
        // }
    }
}